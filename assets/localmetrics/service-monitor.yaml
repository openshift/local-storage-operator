apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: lso-metrics-exporter
  namespace: default
  labels:
    app: lso-metrics-exporter
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: lso-metrics-exporter
  endpoints:
  - port: metrics
    path: /metrics
    interval: 1m
