---
apiVersion: "local.storage.openshift.io/v1"
kind: "LocalVolume"
metadata:
  name: "example"
spec:
  storageClassDevices:
    - storageClassName: "foobar"
      volumeMode: Block
      devicePaths:
        - /dev/nvme1n1

